export default function Cursos() {
  return (
    <section className="max-w-5xl mx-auto px-6 py-24">
      <h1 className="font-display text-4xl mb-6">Cursos</h1>
      <p className="text-ink/70">
        {/* TODO: listar cursos disponibles desde la tabla `courses` (join con `services` para precio) */}
        Próximamente disponible.
      </p>
    </section>
  )
}
